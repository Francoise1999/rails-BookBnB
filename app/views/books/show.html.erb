<p style="color: green"><%= notice %></p>

<%= render @book %>

<h1><%= @book.title %></h1>
<p><%= @book.content %></p>
<img src="<%= @book.image_url %>" alt="">

<div>
  <%= link_to "Modifier les details de ton livre", edit_books_path(@book) %> |
  <%= link_to "Retour a la page principale", book_path %>

  <%= button_to "Enlever ce livre de ta liste", @book, method: :delete %>
</div>
